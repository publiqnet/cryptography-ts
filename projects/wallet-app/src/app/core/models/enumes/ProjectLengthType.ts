export enum ProjectLengthType {
    oneDay = 1,
    oneWeek,
    oneMonth,
    sixMonths,
    oneYear,
    notSpecified
}

export enum InterestedProjectLengthType {
    longTerm = 1,
    midTerm,
    shortTerm,
    all
}
